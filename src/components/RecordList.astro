---
interface Props {
  label: string;
  rows: { dt: string; dd: string }[];
}
const { label, rows } = Astro.props;
---
<section class="record">
  <h2>{label}</h2>
  <dl>
    {rows.map((row) => (
      <>
        <dt>{row.dt}</dt>
        <dd>{row.dd}</dd>
      </>
    ))}
  </dl>
</section>
